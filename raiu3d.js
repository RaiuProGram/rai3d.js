var fov;
var poore1, poore2, pa;
var sinukoht = 0;
var cs1;
function render(nx, ny, nz) {
    pa = Math.atan2(nx, ny) * 180 / Math.PI;
    
}

function angle(x, y) {
    return Math.atan2(x, y) * 180 / Math.PI;
}